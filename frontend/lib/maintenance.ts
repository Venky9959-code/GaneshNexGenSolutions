/**
 * Global Maintenance / Under Process Configuration
 * 
 * When set to `true`, the public website displays the dedicated "Website Under Process" page.
 * No navigation or other pages will open.
 * Internal routes like `/dashboard` and `/admin` remain accessible for administrators.
 * 
 * To restore the full public website, simply change `IS_UNDER_PROCESS` to `false`.
 */
export const IS_UNDER_PROCESS = true;
